import { Appointmentclass } from './appointmentclass';

describe('Appointmentclass', () => {
  it('should create an instance', () => {
    expect(new Appointmentclass()).toBeTruthy();
  });
});
